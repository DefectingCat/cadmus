# 示例配置文件

server:
  port: 8080
  host: localhost

database:
  host: localhost
  port: 5432
  name: cadmus
  user: cadmus
  password: ${DB_PASSWORD}

redis:
  host: localhost
  port: 6379
  password: ${REDIS_PASSWORD}

jwt:
  secret: ${JWT_SECRET}
  expiry: 24h